{
    "default": [
        "concat",
        "jscs",
        "jshint",
        "uglify",
        "notify:build"
    ],

    "sync": [
        "browserSync",
        "watch",
        "notify:watch"
    ]
}
